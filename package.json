{
  "name": "osr-characters",
  "version": "1.0.0",
  "private": true,
  "description": "Character Keeper — tabletop RPG character store + combat tracker for OSR systems and Unknown Armies 3rd Edition (static HTML/CSS/JS app). No build step.",
  "scripts": {
    "test": "node --test",
    "convert-bestiary": "node scripts/convert-bestiary.js",
    "convert-spells": "node scripts/convert-spells-from-shadowdark-resources.js",
    "bundle-standalone": "node scripts/bundle-standalone.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
